
@model IEnumerable<EducationPlatfotm.Domain.Users.Teacher>

<div class="container">
    <h3>Преподаватель</h3>
    <link href="//maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css" rel="stylesheet">

    <a class="btn btn-default" asp-action="Edit">Добавить преподавателя</a>
    <p></p>

    @if (Model.Any())
    {
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th></th>
                    <th></th>
                    <th>Id</th>
                    <th>@Html.DisplayNameFor(model => model.LastName)</th>
                    <th>@Html.DisplayNameFor(model => model.FirstName)</th>
                    <th>@Html.DisplayNameFor(model => model.Patronymic)</th>
                    <th>@Html.DisplayNameFor(model => model.Subjects)</th>
                </tr>
            </thead>

            <tbode>
                @foreach (var teacher in Model)
                {
                    <tr>
                        <td>
                            <a asp-controller="Teacher" asp-action="Details" asp-route-Id="@teacher.Id">
                                Информация
                                <i class="fa fa-id-card" aria-hidden="true"></i>
                            </a>
                        </td>
                        <td><a asp-controller="Teacher" asp-action="Edit" asp-route-Id="@teacher.Id">Редактировать</a></td>
                        <td>@teacher.Id</td>
                        <td>@teacher.LastName</td>
                        <td>@teacher.FirstName</td>
                        <td>@teacher.Patronymic</td>
                        <td>
                        @foreach(var sub in teacher.Subjects)
                        {
                            @if(sub.IsInvolved == true)
                            {
                                <p>@sub.Name</p>
                            }
                        }
                        </td>
                        <td>
                            <a asp-controller="Teacher" asp-action="Delete" asp-route-Id="@teacher.Id">Удалить</a>
                        </td>
                    </tr>
                }
            </tbode>
        </table>
    }
    else
    {
        <p>Сотрудников нет!</p>
    }

    <a asp-controller="Home" asp-action="Index">На главную</a>
</div>

